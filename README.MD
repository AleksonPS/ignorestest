Чтобы удалить файлы в репозитоории, но оставить их локально, так как они случайно были загружены
1) в gitignore прописываем *.log
2) очистим кэш
git rm --cached *.log
3) пушим, в репозитории удаляются файлы с *.log